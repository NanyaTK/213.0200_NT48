<dates>投稿:2025-07-20</dates>

# RV32IMC Part.1（Chiselことはじめ）

## ChiselでRISC-Vを作ろうの会設立
ずっとやろうと思っていたﾔﾂです．全てを知らない状態からスタートします．

## 環境構築
Windows11+WSL2(Ubuntu 22.04LTS)  <br>
この上にDockerでコンテナを適当にたてます<br>
Dockerfileは適当に下の感じ．sbtは入れなくてもvscodeのmetalsで入るような気もする<br>
usbをマウントしているのでそれ用のディレクトリも作成する．
```
FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

ENV LANG=C.UTF-8
ENV LC_ALL=C.UTF-8

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    ca-certificates \
    curl \
    gnupg \
    wget && \
    mkdir -p /etc/apt/keyrings && \
    wget -qO - https://packages.adoptium.net/artifactory/api/gpg/key/public | gpg --dearmor -o /etc/apt/keyrings/adoptium.gpg && \
    echo "deb [signed-by=/etc/apt/keyrings/adoptium.gpg] https://packages.adoptium.net/artifactory/deb jammy main" | tee /etc/apt/sources.list.d/adoptium.list && \
    apt-get update && \
    apt-get install -y --no-install-recommends \
    temurin-11-jdk \
    git \
    build-essential \
    verilator \
    gtkwave && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# sbt
RUN echo "deb https://repo.scala-sbt.org/scalasbt/debian all main" | tee /etc/apt/sources.list.d/sbt.list && \
    curl -sL "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x2EE0EA64E40A89B84B2DF73499E82A75642AC823" | gpg --no-default-keyring --keyring gnupg-ring:/etc/apt/trusted.gpg.d/sbt.gpg --import && \
    chmod 644 /etc/apt/trusted.gpg.d/sbt.gpg && \
    apt-get update && \
    apt-get install -y sbt && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

RUN mkdir /mnt/t
RUN mkdir /mnt/h

CMD ["/bin/bash"]

```

## コンテナでの作業
[chisel-template](https://github.com/chipsalliance/chisel-template/)をクローンしてリポジトリを作ったはいいものの，```sbt test```が通らない．ｺﾆｮｺﾆｮしたら通ると思って15時間が経過したのでchiseltestを採用しました．リポジトリアーカイブされてしまったから使いたくなかったんだけどﾅ．chiselsimがよくわかんなかったので．<br>
build.sbtは以下
```scala
// See README.md for license details.

ThisBuild / scalaVersion     := "2.13.16"
ThisBuild / version          := "0.1.0"
ThisBuild / organization     := "見せられないよ！"

val chiselVersion = "6.7.0"

lazy val root = (project in file("."))
  .settings(
    name := "見せられないよ！",
    libraryDependencies ++= Seq(
      "org.chipsalliance" %% "chisel" % chiselVersion,
      "edu.berkeley.cs" %% "chiseltest" % "6.0.0" % "test",
    ),
    scalacOptions ++= Seq(
      "-language:reflectiveCalls",
      "-deprecation",
      "-feature",
      "-Xcheckinit",
      "-Ymacro-annotations",
    ),
    addCompilerPlugin("org.chipsalliance" % "chisel-plugin" % chiselVersion cross CrossVersion.full),
  )

```
一応念のためGCDSpec.scala
```scala
package gcd

import chisel3._
import chisel3.experimental.BundleLiterals._
import org.scalatest.freespec.AnyFreeSpec
import org.scalatest.matchers.must.Matchers
import chiseltest._

class GCDSpec extends AnyFreeSpec with ChiselScalatestTester with Matchers {

  "Gcd should calculate proper greatest common denominator" in {
    test(new DecoupledGcd(16)) { dut =>
      val testValues = for { x <- 0 to 10; y <- 0 to 10 } yield (x, y)

      dut.reset.poke(true.B)
      dut.clock.step(1)
      dut.reset.poke(false.B)
      dut.clock.step(1)

      var sent = 0
      var received = 0
      var cycles = 0

      while (received < testValues.length) {
        cycles += 1
        assert(cycles < 1000, "Timeout: Simulation took too long")

        if (sent < testValues.length && dut.input.ready.peek().litToBoolean) {
          dut.input.valid.poke(true.B)
          dut.input.bits.value1.poke(testValues(sent)._1.U)
          dut.input.bits.value2.poke(testValues(sent)._2.U)
          sent += 1
        } else {
          dut.input.valid.poke(false.B)
        }

        if (dut.output.valid.peek().litToBoolean) {
          dut.output.ready.poke(true.B)
          val expectedGcd = BigInt(testValues(received)._1).gcd(BigInt(testValues(received)._2))
          dut.output.bits.gcd.expect(expectedGcd.U)
          received += 1
        } else {
          dut.output.ready.poke(false.B)
        }
        dut.clock.step(1)
      }
    }
  }
}
```

## 次回
RV32Iを実装していく